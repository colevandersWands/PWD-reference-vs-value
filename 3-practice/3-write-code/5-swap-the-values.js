"use strict";


console.assert(pairs[0][0] === 1, "pairs[0][0] unchanged");
console.assert(pairs[0][1] === "b", "swapped in from pairs[1]");
console.assert(pairs[1][0] === 2, "pairs[1][0] unchanged");
console.assert(pairs[1][1] === "a", "swapped in from pairs[0]");
console.assert(temp === "a", "temp holds the original value");
