"use strict";

const menu = [
  ["coffee", "tea", "water"],
  ["cake", "pie", "cookie"],
];

const drink = menu[_][_];
console.assert(drink === "tea", "get tea");

const dessert = menu[_][_];
console.assert(dessert === "cookie", "get cookie");

const first = menu[_][_];
console.assert(first === "coffee", "get coffee");
